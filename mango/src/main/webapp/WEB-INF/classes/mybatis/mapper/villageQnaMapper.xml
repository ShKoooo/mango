<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna">

	<insert id="insertBoard" parameterType="com.sp.mango.village.qna.VillageQna">
		INSERT INTO village_bbs(vNum, userId, subject, content, hitCount, regDate, 
				vcNum, areaNum, addr2, vBlat, vBlon)
		VALUES (village_bbs_seq.NEXTVAL, #{userId}, #{subject}, #{content}, 0, SYSDATE, 
		1, #{areaNum}, #{addr2, jdbcType=VARCHAR}, #{vBlat, jdbcType=DOUBLE}, #{vBlon, jdbcType=DOUBLE})
	</insert>
	
	<!-- 이미지 첨부
	<insert id="insertImg" parameterType="com.sp.mango.village.qna.VillageQna">
		INSERT INTO villageImg (vimgNum, vimgSaveFilename, vimgOrigFilename, vNum)
		VALUES (villageImg_seq.NEXTVAL, #{vimgSaveFilename}, #{vimgOrigFilename}, 1)
	</insert>
	-->
	
	<sql id="where-list">
		<choose>
			<when test="condition == 'all'">
				( INSTR(subject, #{keyword}) &gt; 0
					OR DBMS_LOB.INSTR(content, #{keyword}) &gt; 0 )
			</when>
			<when test="condition == 'content'">
				DBMS_LOB.INSTR(content, #{keyword}) &gt; 0
			</when>
			<otherwise>
			    INSTR(${condition}, #{keyword}) &gt; 0
			</otherwise>
		</choose>
	</sql>
	
	
	<select id="dataCount" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*), 0)
		FROM village_bbs v
		JOIN member m ON v.userId = m.userId
		<where>
			<if test="keyword!=null and keyword!='' ">
				<include refid="where-list"/>
			</if>
		</where>
	</select>

	<!-- 회원이 등록한 주소 및 주소가 속한 지역 -->
	<select id="listMemberAddr" parameterType="String" resultType="com.sp.mango.village.MemberAddr">
		SELECT maNum, maLat, maLon, maAddr1, maAddr2, a.areaNum, a.area1, a.area2, a.area3, a.aLat, a.aLon
		FROM memberAddr ma
		JOIN area a ON ma.areaNum = a.areaNum
		WHERE userId = #{userId}
	</select>
	
	<!-- 게시글 리스트 : 전체 -->
	<select id="listBoard" parameterType="map" resultType="com.sp.mango.village.qna.VillageQna">
		SELECT * FROM (
			SELECT ROWNUM rnum, tb.* FROM (
				SELECT vNum, v.userId, subject,
					TO_CHAR(v.regDate, 'YYYY-MM-DD') reg_date,
					hitCount, vcNum, area3
				FROM village_bbs v
				JOIN area a ON v.areaNum = a.areaNum
				<where>
				vcNum = 1
					<if test="keyword != null and keyword != ''">
						AND <include refid="where-list"/>
					</if>
				</where>
				ORDER BY vNum DESC
			) tb WHERE ROWNUM &lt;= #{end}
		) WHERE rnum &gt;= #{start}
	</select>
	
	<!-- 게시글 리스트(회원) : 회원 지정 위치 기준 반경 5km 게시글-->
	<select id="memberListBoard" parameterType="map" resultType="com.sp.mango.village.qna.VillageQna">
		SELECT * FROM (
			SELECT ROWNUM rnum, tb.* FROM (
				SELECT vNum, v.userId, subject,
					TO_CHAR(v.regDate, 'YYYY-MM-DD') reg_date, 	
					hitCount, vcNum, area3
				FROM village_bbs v
				JOIN area a ON v.areaNum = a.areaNum
				<where>
				vcNum = 1 
					<if test="keyword != null and keyword != ''">
						AND <include refid="where-list"/>
					</if>
				AND v.areaNum IN (
					  SELECT areaNum
					  FROM (
						SELECT * FROM (
							SELECT areaNum,
								(6371*acos(cos(#{maLat}*3.14159265358979/180)*cos(aLat*3.14159265358979/180)*cos(aLon*3.14159265358979/180
                        		-#{maLon}*3.14159265358979/180)+sin(#{maLat}*3.14159265358979/180)*sin(aLat*3.14159265358979/180)))
                        		AS distance
                        		FROM area ar
                    		) WHERE distance &lt;= 5   
						)
					)
				</where>
				ORDER BY vNum DESC
			) tb WHERE ROWNUM &lt;=#{end}
		) WHERE rnum &gt;=#{start}
	</select>
	
	<!-- 회원이 등록한 주소 개수 세기 -->
	<select id="memAddrCount" parameterType="String" resultType="Integer">
		SELECT COUNT(maAddr1) FROM memberAddr WHERE userId=#{userId}
	</select>
	
	<select id="readBoard" parameterType="Integer" resultType="com.sp.mango.village.qna.VillageQna">
		SELECT vNum, v.userId, userNickName, subject, areaNum,
			content, TO_CHAR(v.regDate, 'YYYY-MM-DD') reg_date, hitCount
		FROM village_bbs v
		JOIN member m ON v.userId = m.userId
		WHERE vNum=#{vNum}
	</select>
	
	<update id="updateHitCount" parameterType="Integer">
		UPDATE village_bbs SET hitCount=hitCount+1 WHERE vNum=#{vNum}
	</update>
	
	<!-- 이전글, 다음글 -->
	<select id="preReadBoard" parameterType="map" resultType="com.sp.mango.village.qna.VillageQna">
		SELECT * FROM (
			SELECT vNum, subject
			FROM village_bbs v
			JOIN member m ON v.userId = m.userId
			<where>
				<if test="keyword != null and keyword != '' ">
					<include refid="where-list"/>
				</if>
				AND (vNum &lt; #{vNum})
			</where>
			ORDER BY vNum DESC
		) WHERE ROWNUM = 1
	</select>
	
	<select id="nextReadBoard" parameterType="map" resultType="com.sp.mango.village.qna.VillageQna">
		SELECT * FROM (
			SELECT vNum, subject
			FROM village_bbs v
			JOIN member m ON v.userId = m.userId
			<where>
				<if test="keyword != null and keyword != '' ">
					<include refid="where-list"/>
				</if>
				AND (vNum &gt; #{vNum})
			</where>
			ORDER BY vNum ASC
		) WHERE ROWNUM = 1
	</select>
	
	
	<update id="updateBoard" parameterType="com.sp.mango.village.qna.VillageQna">
		UPDATE village_bbs SET subject=#{subject}, content=#{content}, areaNum=#{areaNum},
			vBlat=#{vBlat, jdbcType=DOUBLE}, vBlon=#{vBlon, jdbcType=DOUBLE}
		WHERE vNum = #{vNum}
	</update>
	
	<delete id="deleteBoard" parameterType="com.sp.mango.village.qna.VillageQna">
		DELETE FROM village_bbs WHERE vNum = #{vNum}
	</delete>

	<!-- 좋아요 추가 -->
	<insert id="insertBoardLike" parameterType="map">
		INSERT INTO vbbsLike(vNum, userId) VALUES (#{vNum}, #{userId})
	</insert> 
	
	<!-- 좋아요 삭제 -->
	<delete id="deleteBoardLike" parameterType="map">
		DELETE FROM vbbsLike WHERE vNum = #{vNum} AND userId = #{userId}
	</delete>
	
	<!-- 게시글 좋아요 개수 -->
	<select id="boardLikeCount" parameterType="Integer" resultType="Integer">
		SELECT COUNT(*) FROM vbbsLike WHERE vNum=#{vNum}
	</select>
	
	<!-- 게시글 좋아요 여부 -->
	<select id="userBoardLiked" parameterType="map" resultType="com.sp.mango.village.qna.VillageQna">
		SELECT vNum, userId FROM vbbsLike WHERE vNum = #{vNum} AND userId = #{userId}
	</select>
	
	
	
	<!-- 댓글 -->
	<insert id="insertReply" parameterType="com.sp.mango.village.qna.Reply">
	
	</insert>
</mapper>