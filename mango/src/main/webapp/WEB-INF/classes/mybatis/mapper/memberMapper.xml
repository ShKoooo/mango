<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="loginMember" parameterType="String" resultType="com.sp.mango.member.Member">
		SELECT userId, userPwd, userName, userNickName, regDate, update_Date
		FROM member
		WHERE userId = #{userId} AND memberEnable != 0
	</select>
	
	<sql id="where-duplMember">
		<choose>
			<when test="chkWay=='id'">
			</when>
			<when test="chkWay=='nick'">
			</when>
		</choose>
	</sql>
	
	<select id="userDuplCheck" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*),0)
		FROM member
		WHERE ${chkWay} = #{userParam}
	</select>
	
	<select id="readMember" parameterType="String" resultType="com.sp.mango.member.Member">
		
	</select>
</mapper>